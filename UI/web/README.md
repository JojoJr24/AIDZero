# Web UI

Browser-based interface for the AIDZero agent creator.

Run:

```bash
uv run AIDZero.py --ui web
```

Custom host/port:

```bash
uv run AIDZero.py --ui web --ui-option host=0.0.0.0 --ui-option port=8787
```

Then open the URL printed in the terminal (default `http://127.0.0.1:8787`).
